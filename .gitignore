node_modules/
dist/
build/
.next/
out/

# Variáveis de ambiente — chave de pagamento e segredo de sessão passam por
# aqui, e é exatamente o tipo de arquivo que se commita sem querer uma vez só.
.env
.env.*
!.env.exemplo

*.log
npm-debug.log*
.DS_Store
Thumbs.db

.vercel/
coverage/
*.tsbuildinfo

# Dados de personagem gravados ao rodar o servidor local. São de quem jogou
# na máquina, não do projeto — e entraram uma vez sem querer.
apps/*/dados/
